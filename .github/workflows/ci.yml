name: CI

on:
  push:
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Ruff format
        run: uv run ruff format --check pakkenellik tests

      - name: Ruff lint
        run: uv run ruff check pakkenellik tests

      - name: Mypy
        run: uv run mypy --config-file pyproject.toml pakkenellik

      - name: Tests
        run: uv run pytest

      - name: Build
        run: uv build
